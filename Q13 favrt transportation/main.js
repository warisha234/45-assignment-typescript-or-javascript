"use strict";
let favrtTransport = ['sport bike', 'Green bus', 'Honda civic', 'Train'];
for (let i = 0; i < favrtTransport.length; i++) {
    console.log('I would like to own a  ' + favrtTransport[i]);
}
